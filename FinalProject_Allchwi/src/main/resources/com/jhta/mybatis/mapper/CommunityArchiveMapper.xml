<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jhta.mybatis.mapper.CommunityArchiveMapper">
	<select id="list" resultType="com.jhta.allchwi.vo.community.archive.ArchiveVO" parameterType="hashmap">
		select * from ref_room
		limit #{startRow},6
	</select>
	
	<select id="count" resultType="int">
		select nvl(count(*),0) cnt from  ref_room
	</select>
	
	
	
</mapper>